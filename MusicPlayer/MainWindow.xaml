<Window x:Class="MusicPlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MusicPlayer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <DockPanel>
            <StackPanel DockPanel.Dock="Left" Background="LightGray" HorizontalAlignment="Left" VerticalAlignment="Stretch" Width="167" Margin="0">
                <Button x:Name="Playlist" Content="Playlist" Background="{x:Null}" BorderBrush="Black" Click="Playlist_Click"/>
                <Separator Opacity="0" Height="5"/>
                <Button x:Name="Files" Content="Files" Background="{x:Null}" BorderBrush="Black" Click="Files_Click"/>
                <Separator Opacity="0" Height="60" />
                <TextBox x:Name="PathInput" Height="23" TextWrapping="Wrap" FontSize="10" Text="Path " HorizontalAlignment="Center" Width="167"/>
                <Button x:Name="OkBtn" Content="OK" Click="OkBtn_Click"/>
                <Button x:Name="SaveBtn" Content="Save" Click="SaveBtn_Click"/>
                <Button x:Name="LoadBtn" Content="Load" Click="LoadBtn_Click"/>
            </StackPanel>

            <StackPanel DockPanel.Dock="Bottom" Margin="10" Background="Gray">
                <Slider Name="ClipProgress" Thumb.DragStarted="ClipProgress_DragStarted"  Thumb.DragCompleted="ClipProgress_DragCompleted" ValueChanged="ClipProgress_ValueChanged" IsMoveToPointEnabled="True" AutoToolTipPlacement="BottomRight" ToolTipOpening="ClipProgress_ToolTipOpening" />
                <Label Name="lblStatus" Content="Not playing..." HorizontalContentAlignment="Center" Margin="5" Background="White" />

                <UniformGrid Columns="4" VerticalAlignment="Center" Rows="1" HorizontalAlignment="Center">
                    <Button x:Name="BtnBack" Content="Back" Click="BtnBack_Click" Width="70" VerticalAlignment="Center">
                    </Button>
                    <Button x:Name="BtnPlay" Content="Play" Click="BtnPlay_Click" Width="70" VerticalAlignment="Center">
                    </Button>
                    <Button Content="Stop" x:Name="BtnStop" Click="BtnStop_Click" Width="70" VerticalAlignment="Center"/>
                    <Button Content="Skip" x:Name="BtnSkip" Click="BtnSkip_Click" Width="70" VerticalAlignment="Center"/>
                </UniformGrid>

            </StackPanel>

            <ListView x:Name="AllMusicTracks" MouseWheel="AllMusicTracks_MouseWheel" AllowDrop="True" Drop="AllMusicTracks_Drop" DockPanel.Dock="Top" Width="Auto" MinWidth="622" MinHeight="330" MouseDoubleClick="AllMusicTracks_MouseDoubleClick">
                <ListView.View>
                    <GridView>
                        <GridViewColumn DisplayMemberBinding="{Binding ID}" Width="45">
                            <GridViewColumn.Header>
                                <GridViewColumnHeader Tag="ID" Click="AllMusicTracksColumn_Click">ID</GridViewColumnHeader>
                            </GridViewColumn.Header>
                        </GridViewColumn>
                        <GridViewColumn DisplayMemberBinding="{Binding SongName}" Width="385">
                            <GridViewColumn.Header>
                                <GridViewColumnHeader Tag="SongName" Click="AllMusicTracksColumn_Click">Song</GridViewColumnHeader>
                            </GridViewColumn.Header>
                        </GridViewColumn>
                        <GridViewColumn x:Name="ArtistTab" DisplayMemberBinding="{Binding Artist}" Width="155">
                            <GridViewColumn.Header>
                                <GridViewColumnHeader Tag="Artist" Click="AllMusicTracksColumn_Click">Artist</GridViewColumnHeader>
                            </GridViewColumn.Header>
                        </GridViewColumn>
                        <GridViewColumn DisplayMemberBinding="{Binding Extension}" Width="55">
                            <GridViewColumn.Header>
                                <GridViewColumnHeader Tag="Extension" Click="AllMusicTracksColumn_Click">Extension</GridViewColumnHeader>
                            </GridViewColumn.Header>
                        </GridViewColumn>
                    </GridView>
                </ListView.View>
            </ListView>
        </DockPanel>
    </Grid>
</Window>
